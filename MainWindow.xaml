<Window x:Class="FinancialPlanner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:FinancialPlanner.ViewModels"
        xmlns:models="clr-namespace:FinancialPlanner.Models"
        xmlns:converters="clr-namespace:FinancialPlanner.Converters"
        mc:Ignorable="d"
        Title="Финансовый Планировщик и Daily Tracker" 
        Height="800" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">
    <Window.Resources>
        <converters:TransactionColorConverter x:Key="TransactionColorConverter"/>
        <converters:TransactionBorderConverter x:Key="TransactionBorderConverter"/>
        <converters:BalanceColorConverter x:Key="BalanceColorConverter"/>
        <converters:StrikeThroughConverter x:Key="StrikeThroughConverter"/>
    </Window.Resources>
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}" Padding="20">
            <StackPanel>
                <TextBlock Text="💰 Финансовый Планировщик" 
                          FontSize="28" FontWeight="Bold" 
                          Foreground="White" Margin="0,0,0,10"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Доходы" FontSize="12" Foreground="White" Opacity="0.9"/>
                        <TextBlock Text="{Binding TotalIncome, StringFormat='{}{0:C}'}" 
                                  FontSize="20" FontWeight="Bold" Foreground="White"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Расходы" FontSize="12" Foreground="White" Opacity="0.9"/>
                        <TextBlock Text="{Binding TotalExpenses, StringFormat='{}{0:C}'}" 
                                  FontSize="20" FontWeight="Bold" Foreground="White"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Баланс" FontSize="12" Foreground="White" Opacity="0.9"/>
                        <TextBlock Text="{Binding Balance, StringFormat='{}{0:C}'}" 
                                  FontSize="20" FontWeight="Bold" 
                                  Foreground="{Binding Balance, Converter={StaticResource BalanceColorConverter}}"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Main Content with Tabs -->
        <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border Name="Border" Background="Transparent" 
                                       BorderThickness="0" Padding="20,10">
                                    <ContentPresenter ContentSource="Header" 
                                                    HorizontalAlignment="Center" 
                                                    VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="False">
                                        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="FontSize" Value="16"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </Style>
            </TabControl.Resources>

            <!-- Финансы Tab -->
            <TabItem Header="💵 Финансы">
                <Grid Margin="20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="350"/>
                    </Grid.ColumnDefinitions>

                    <!-- Transactions List -->
                    <Border Grid.Column="0" Background="White" 
                           CornerRadius="10" Padding="15" 
                           Effect="{StaticResource ShadowEffect}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Text="Транзакции" 
                                      FontSize="20" FontWeight="Bold" 
                                      Margin="0,0,0,15"/>

                            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                                <ItemsControl ItemsSource="{Binding Transactions}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate DataType="{x:Type models:Transaction}">
                                            <Border Background="{Binding Type, Converter={StaticResource TransactionColorConverter}}" 
                                                   CornerRadius="8" Padding="15" Margin="0,0,0,10"
                                                   BorderThickness="0,0,4,0"
                                                   BorderBrush="{Binding Type, Converter={StaticResource TransactionBorderConverter}}">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <StackPanel Grid.Column="0">
                                                        <TextBlock Text="{Binding Description}" 
                                                                  FontSize="16" FontWeight="SemiBold"/>
                                                        <TextBlock Text="{Binding Category}" 
                                                                  FontSize="12" Opacity="0.7" Margin="0,5,0,0"/>
                                                        <TextBlock Text="{Binding Date, StringFormat='{}{0:dd.MM.yyyy HH:mm}'}" 
                                                                  FontSize="11" Opacity="0.6" Margin="0,2,0,0"/>
                                                    </StackPanel>
                                                    <TextBlock Grid.Column="1" 
                                                              Text="{Binding Amount, StringFormat='{}{0:C}'}" 
                                                              FontSize="18" FontWeight="Bold"
                                                              Margin="15,0"/>
                                                    <Button Grid.Column="2" 
                                                           Content="✕" 
                                                           Command="{Binding DataContext.DeleteTransactionCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                           CommandParameter="{Binding}"
                                                           Style="{StaticResource IconButtonStyle}"
                                                           Width="30" Height="30"/>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Grid>
                    </Border>

                    <!-- Add Transaction Form -->
                    <Border Grid.Column="2" Background="White" 
                           CornerRadius="10" Padding="20" 
                           Effect="{StaticResource ShadowEffect}">
                        <StackPanel>
                            <TextBlock Text="Новая транзакция" 
                                      FontSize="20" FontWeight="Bold" 
                                      Margin="0,0,0,20"/>

                            <ComboBox SelectedItem="{Binding NewTransactionType}" 
                                     Margin="0,0,0,15" 
                                     Style="{StaticResource ComboBoxStyle}">
                                <ComboBox.Items>
                                    <models:TransactionType>Income</models:TransactionType>
                                    <models:TransactionType>Expense</models:TransactionType>
                                </ComboBox.Items>
                            </ComboBox>

                            <TextBox Text="{Binding NewTransactionDescription}" 
                                    Style="{StaticResource TextBoxStyle}"
                                    Margin="0,0,0,15"
                                    Tag="Описание"/>

                            <TextBox Text="{Binding NewTransactionAmount}" 
                                    Style="{StaticResource TextBoxStyle}"
                                    Margin="0,0,0,15"
                                    Tag="Сумма"/>

                            <TextBox Text="{Binding NewTransactionCategory}" 
                                    Style="{StaticResource TextBoxStyle}"
                                    Margin="0,0,0,20"
                                    Tag="Категория"/>

                            <Button Content="Добавить" 
                                   Command="{Binding AddTransactionCommand}"
                                   Style="{StaticResource PrimaryButtonStyle}"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>

            <!-- Daily Tracker Tab -->
            <TabItem Header="📅 Daily Tracker">
                <Grid Margin="20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="350"/>
                    </Grid.ColumnDefinitions>

                    <!-- Daily Entry Content -->
                    <Border Grid.Column="0" Background="White" 
                           CornerRadius="10" Padding="20" 
                           Effect="{StaticResource ShadowEffect}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" 
                                      Text="{Binding SelectedDailyEntry.Date, StringFormat='{}{0:dd MMMM yyyy}'}" 
                                      FontSize="24" FontWeight="Bold" 
                                      Margin="0,0,0,20"/>

                            <!-- Mood -->
                            <StackPanel Grid.Row="1" Margin="0,0,0,20">
                                <TextBlock Text="Настроение" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>
                                <Slider Value="{Binding DailyMood}" 
                                       Minimum="1" Maximum="10" 
                                       TickFrequency="1" IsSnapToTickEnabled="True"
                                       Style="{StaticResource SliderStyle}"/>
                                <TextBlock Text="{Binding DailyMood}" 
                                         FontSize="18" FontWeight="Bold" 
                                         HorizontalAlignment="Center" 
                                         Foreground="{StaticResource PrimaryBrush}"/>
                            </StackPanel>

                            <!-- Habits -->
                            <StackPanel Grid.Row="2" Margin="0,0,0,20">
                                <TextBlock Text="Привычки" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>
                                <ItemsControl ItemsSource="{Binding SelectedDailyEntry.Habits}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <CheckBox Content="{Binding HabitName}" 
                                                     IsChecked="{Binding IsCompleted}"
                                                     Command="{Binding DataContext.ToggleHabitCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                     CommandParameter="{Binding}"
                                                     FontSize="14" Margin="0,5"
                                                     Style="{StaticResource CheckBoxStyle}"/>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>

                            <!-- Tasks -->
                            <StackPanel Grid.Row="3">
                                <TextBlock Text="Задачи" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>
                                <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="300">
                                    <ItemsControl ItemsSource="{Binding SelectedDailyEntry.Tasks}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="{StaticResource LightBackgroundBrush}" 
                                                       CornerRadius="8" Padding="12" Margin="0,0,0,8">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <CheckBox Grid.Column="0" 
                                                                 IsChecked="{Binding IsCompleted}"
                                                                 Command="{Binding DataContext.ToggleTaskCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                                 CommandParameter="{Binding}"
                                                                 Margin="0,0,10,0"/>
                                                        <TextBlock Grid.Column="1" 
                                                                  Text="{Binding Description}" 
                                                                  FontSize="14"
                                                                  TextDecorations="{Binding IsCompleted, Converter={StaticResource StrikeThroughConverter}}"/>
                                                        <Button Grid.Column="2" 
                                                               Content="✕" 
                                                               Command="{Binding DataContext.DeleteTaskCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                               CommandParameter="{Binding}"
                                                               Style="{StaticResource IconButtonStyle}"
                                                               Width="24" Height="24"/>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Side Panel -->
                    <Border Grid.Column="2" Background="White" 
                           CornerRadius="10" Padding="20" 
                           Effect="{StaticResource ShadowEffect}">
                        <StackPanel>
                            <TextBlock Text="Управление" 
                                      FontSize="20" FontWeight="Bold" 
                                      Margin="0,0,0,20"/>

                            <!-- Notes -->
                            <TextBlock Text="Заметки" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,10"/>
                            <TextBox Text="{Binding DailyNotes}" 
                                    Style="{StaticResource TextBoxStyle}"
                                    TextWrapping="Wrap" 
                                    AcceptsReturn="True"
                                    Height="100"
                                    Margin="0,0,0,20"
                                    Tag="Ваши заметки..."/>

                            <!-- Add Habit -->
                            <TextBlock Text="Новая привычка" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,10"/>
                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0" 
                                        Text="{Binding NewHabitName}" 
                                        Style="{StaticResource TextBoxStyle}"
                                        Tag="Название привычки"/>
                                <Button Grid.Column="1" 
                                       Content="+" 
                                       Command="{Binding AddHabitCommand}"
                                       Style="{StaticResource IconButtonStyle}"
                                       Width="40" Height="40" Margin="10,0,0,0"/>
                            </Grid>

                            <!-- Habits List -->
                            <TextBlock Text="Все привычки" FontSize="14" FontWeight="SemiBold" Margin="0,10,0,10"/>
                            <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="150">
                                <ItemsControl ItemsSource="{Binding AvailableHabits}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="{StaticResource LightBackgroundBrush}" 
                                                   CornerRadius="6" Padding="10" Margin="0,0,0,5">
                                                <Grid>
                                                    <TextBlock Text="{Binding}" FontSize="13"/>
                                                    <Button HorizontalAlignment="Right" 
                                                           Content="✕" 
                                                           Command="{Binding DataContext.DeleteHabitCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                           CommandParameter="{Binding}"
                                                           Style="{StaticResource IconButtonStyle}"
                                                           Width="20" Height="20"/>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>

                            <!-- Add Task -->
                            <TextBlock Text="Новая задача" FontSize="14" FontWeight="SemiBold" Margin="0,20,0,10"/>
                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0" 
                                        Text="{Binding NewTaskDescription}" 
                                        Style="{StaticResource TextBoxStyle}"
                                        Tag="Описание задачи"/>
                                <Button Grid.Column="1" 
                                       Content="+" 
                                       Command="{Binding AddTaskCommand}"
                                       Style="{StaticResource IconButtonStyle}"
                                       Width="40" Height="40" Margin="10,0,0,0"/>
                            </Grid>

                            <Button Content="Сохранить" 
                                   Command="{Binding SaveDailyEntryCommand}"
                                   Style="{StaticResource PrimaryButtonStyle}"
                                   Margin="0,20,0,0"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
